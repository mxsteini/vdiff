#
test1:
# target is optional and inserted from configuration.default.target
  target:
    _reference: "https://bain.localhost"
    new: "https://new.project.com"
    dev: "https://dev.project.com"
    local: "https://bain.localhost"
  # target is optional and inserted from configuration.default.target if available
  initialActions:
    -
      path: "/"
      steps:
        -
          action: ""
          waitFor: 800
        -
          action: "click"
          action_selector: ".cookies-text button.close"
          waitFor: 300
  list:
    # single path
    - "/impressum"
    # path with steps
    - path: "/"
      steps:
        -
          # hover, click, focus, key, press,
          action: "focus"
          action_selector: "#quicksearch-sword"
          waitFor: 100
        -
          action: "type"
          action_input: "sparen"
          waitFor: 100
        -
          action: "click"
          action_selector: "#quicksearch > form > button[type=submit]"
          waitFor: 2000
# short version
test2:
  list:
    - /contact.html
    - /aboutus.html